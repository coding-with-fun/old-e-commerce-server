import env from '../../env';

export const ADMIN_PANEL_ENDPOINTS = {
    'forgot-password': 'forgot-password',
    'reset-password': 'reset-password',
    'verify-email': 'verify-email',
};

export const STORAGE_PATH = env.app.root_dir + '/storage/uploads';
